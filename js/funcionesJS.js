// Funcion para el boton de volver arriba de la pagina
function arriba(){ window.scroll(0,0); }

// Funcion para volver atras en el historial del navegador
function volver(){ javascript:history.go(-1); }
